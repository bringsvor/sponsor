-
  !record {model: sponsor, id: sponsor3}:
    name: 'Doffen Duck'
-

-
  Check the wizard
-
  !record {model: add_sponsorship_wizard, id: add_wiz1}:
    sponsor_id: sponsor3
    start_date: '2012-01-01'
    child_id: child2
-
    Add the child
-
    !python {model: add_sponsorship_wizard}: |
      self.data_save(cr, uid, [ref('add_wiz1')], {'lang': 'en_US',
      'search_disable_custom_filters': True, 'tz': False, 'uid': 1, 'active_model': 'sponsor',
      'active_ids': [ref('sponsor1')], 'active_id': ref('sponsor1')})
-
  Check that it was added
-
  !assert {model: sponsored_child, id: child2, string = 'Child must have two sponsors'}:
  - len(sponsors) == 3
-
  Check the order again
-
  !assert {model: sponsored_child, id: child2, string = 'The first added should come first'}:
  - sponsors[0].sponsor_id.name == 'Doffen Duck'
